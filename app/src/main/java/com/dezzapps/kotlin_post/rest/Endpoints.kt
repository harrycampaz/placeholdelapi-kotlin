package com.dezzapps.kotlin_post.rest

class Endpoints {

    companion object {
        const val URL_BASE = "https://jsonplaceholder.typicode.com"
        const val GET_USERS = "/users"
        const val GET_POST_USER = "/posts?"
    }
}